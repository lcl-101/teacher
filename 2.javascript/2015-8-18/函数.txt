JavaScript代码运行环境
环境：教室就是一个环境
宿主环境：JavaScript运行在浏览器 
          运行环境 就是浏览器

执行环境：
   执行环境决定了，变量  函数的 访问的权限
   1.全局环境当前（html）页面就是一个全局环境
     声明的变量   全局变量
   2.函数环境 一个函数就是一个函数环境
     声明的变量   局部变量

   全局环境 不能访问 函数环境  变量 函数
   函数环境 可以访问 全局环境  变量 函数
JavaScript预解析顺序
  1.预解析  预先 读取 代码  把变量=undefined  function 函数放到内存中

  2.执行： 执行  变量=赋值  aa()    函数的执行    
1.script块 依次 去解析
2.解析代码运行的环境
   全局环境 
   函数环境 
3.把 变量  函数 解析到对应的环境下面
   全局环境下：
   全局变量
   function  a(){
   }

4.如果还有<script></script>块再按照上面的步骤依次解析。

回调函数
通过函数的指针来调用函数
把一个函数的指针做为另一个函数的参数，当调用这个
参数的时候，这个函数就叫做回调函数。



JavaScript递归函数

在函数内部  直接或者 间接的调用自己


5的阶乘   5*4*3*2*1









